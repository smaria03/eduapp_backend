en:
  activerecord:
    errors:
      models:
        school_class:
          attributes:
            name:
              invalid_class_format: "must be in the format 1A to 12G"
            subjects:
              taken: "already assigned to this class"
        attendance:
          attributes:
            user_id:
              already_recorded_for_slot: "already has attendance recorded for this class/period/date"
        homework_submission:
          attributes:
            student:
              already_submitted: has already submitted this homework